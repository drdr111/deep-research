def fetch_chunks(topic: str) -> list:
    return [
        f"Информация о {topic} — источник A",
        f"Детали по {topic} — источник B",
        f"Контекст по {topic} — источник C",
        f"Цифры и факты по {topic} — источник D",
        f"Мнение эксперта по {topic} — источник E"
    ]
